PORT=4050
JWT_SECRET=bmgenie-crm-change-me-in-production
NODE_ENV=development
# Shared secret — must match BMGenie backend CRM_INGEST_API_KEY
CRM_INGEST_API_KEY=dev-local-crm-ingest-key-change-me
# Optional: absolute path to SQLite file (default: backend/data/crm.db)
# DATABASE_PATH=
